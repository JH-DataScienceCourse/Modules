hdata <- read.csv("~/IES/Data Science/Curso/Modulo-2(R)/hospital-data.csv", colClasses = "character", header=FALSE) 

best <- function(state, outcome) {
        ocm <- read.csv("outcome-of-care-measures.csv", colClasses = "character", header=FALSE)
                
        
        
}